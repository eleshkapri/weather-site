/**
 * Atmosphere Weather - Configuration Template
 * 
 * To use a custom geocoding API key for enhanced accuracy (e.g., OpenWeatherMap):
 * 1. Copy this file and rename it to 'config.js' in the same directory:
 *      assets/js/config.js
 * 2. Paste your API key below into OPENWEATHER_API_KEY.
 * 
 * 🔒 SECURITY NOTICE:
 * 'assets/js/config.js' is strictly added to .gitignore. It will NEVER be
 * committed or uploaded to your public GitHub repository.
 */

window.CONFIG = window.CONFIG || {};
window.CONFIG.OPENWEATHER_API_KEY = ""; // Paste your 32-character key in config.js (not here)
